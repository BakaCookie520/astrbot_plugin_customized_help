# 压缩饼干 使用帮助

## 主动触发

### 获取服务器运行状态

- `/status` 服务状态

### 今日运势

- `/jrrs` 获取今日运势

- `/jrrs last` 获取今日运势背景图片（需要先获取今日运势）

### 今日老婆

#### 普通用户
- `/今日老婆` 随机抽取一名今日老婆

- `/强娶 @用户` 消耗次数强制与指定用户建立羁绊

- `/关系图`  生成并发送本群今日的老婆关系网络图

- `/我的老婆` 查看今天抽到的记录及次数

- `/rbq排行` 展示近30天被强娶的次数排行（只显示前10名）

- `/抽老婆帮助` 查看详细指令说明

#### 管理员

- `/重置记录` 清空所有今日抽取记录

- `/重置强娶时间` 清空当前群的强娶时间戳

### 点歌

#### 点歌

- `/点歌 <歌名>` 使用默认平台点歌（默认网易云音乐）

- `/<平台名>点歌 <歌名>` 指定平台点歌（未经测试，不保证可用性）

- `/点歌 <歌名> <序号>` 直接选择搜索结果

#### 歌单

- `/歌单收藏 <歌名>` 收藏歌曲

- `/歌单取藏 <歌名>` 取消收藏

- `/歌单列表` 查看个人歌单

- `/歌单点歌 <序号>` 	播放歌单歌曲

**注意！**

**1.歌单按用户独立存储**

**2.默认最多 50 首**

### 陪伴/夸夸（是贴心BOT！）

- `/subscribecm` 订阅每日问候推送

- `/unsubscribecm` 取消每日问候推送

- `/praise` 手动触发夸夸对话

### meme生成

- `{关键词}` 触发meme合成

- `/meme帮助` 查看所有能触发meme合成的关键词

- `/meme详情 xxx` 具体查看某个meme的参数

- `启用meme xxx` 启用指定meme

- `/禁用meme xxx` 禁用指定meme

- `/meme黑名单` 查看哪些meme被禁用了

**提示：**
- 本插件支持从原始消息中提取参数，请用空格隔开参数，如 “喜报 nmsl”；

- 本插件支持从引用消息中提取参数，如“[引用的消息] 喜报”；

- 本插件支持获取任意 QQ 号的头像作为参数，如“踩 @114514”；

- 提供的参数不够时，插件自动获取消息发送者、被 @ 的用户以及 bot 自身的相关参数来补充。

### 跟我说

- `/跟我说 xxx` 让BOT复读xxx的内容

### 赞我

- `/赞我` 给用户点赞（可不带前缀）

- `赞@XXX` 给用户点赞，可以同时@多个人

- `/订阅点赞` 订阅后bot每天自动为用户点赞

- `/取消订阅点赞` 取消订阅后bot每天不再自动为用户点赞

- `/谁赞了bot` 查看谁赞了bot

### BiliBili信息订阅

- `/bili_sub <B站UID> [过滤器...]` 订阅指定 UP 主的动态。可以添加多个过滤器（以空格分隔）以排除不感兴趣的内容

- `/bili_sub_list` 显示当前会话的所有订阅

- `/bili_sub_del <B站UID>` 删除当前会话中对指定 UP 主的订阅

#### 过滤器说明

过滤器可以是以下几种类型：

  - `forward`：过滤掉转发动态。
  - `lottery`：过滤掉互动抽奖动态。
  - `video`：过滤掉视频发布动态。
  - `article`：过滤掉专栏动态。
  - `draw`：过滤掉图文动态。
  - `live`：过滤掉直播动态。
  - `forward_lottery`：过滤掉转发的互动抽奖动态。
  - **正则表达式**：任何不属于上述关键字的字符串都将被视为正则表达式，用于过滤动态文本内容。

**示例**：
`/订阅动态 123456 lottery 关注`
`/bili_sub 123456 lottery 关注`
这条指令会订阅 UID 为 `123456` 的 UP 主，但会过滤掉**抽奖动态**以及动态内容中包含“**关注**”二字的动态。

> **提示**：该指令也用于更新已订阅 UP 主的过滤条件。

### 数据查询

#### BanG！Dream！

##### 普通用户
###### 基础命令

- `/bd` 显示主菜单显示帮助信息

- `/bd help` 显示帮助信息

- `/bd event` 查询当期活动（默认国服）

- `/bd event jp` 查询日服当期活动

- `/bd event <ID>` 查询指定ID的活动

###### 卡面查询

- `/bd <角色名>` 查询角色的所有卡面

- `/bd <别名>` 使用别名查询角色卡面

- `/bd id <ID>` 查看指定ID的卡面大图

- `/bd event <ID>` 查询指定ID的活动

###### 生日查询

- `/bd 生日` 查看今天过生日的角色

- `/bd 生日 <角色名>` 查看角色的生日信息

- `/bd birthday <别名>` 使用别名查询角色生日

##### 管理员

- `/bd admin` 进入管理菜单

- `/bd admin cache` 缓存管理

- `/bd admin subscribers` 查看订阅列表

- `/bd subscribe` 订阅播报

- `/bd unsubscribe` 取消订阅
## 被动触发

1. 戳一戳BOT：触发LLM回复或设定好的回复

2. 复读：BOT会跟随群内进行复读（或“打断施法！”）

3. BiliBili链接解析：在群内发送哔哩哔哩小程序会自动解析并发送原链接和视频详细信息

4. 好感度：你们猜

